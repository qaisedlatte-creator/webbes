import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Static files in public/ are served automatically.
  // No trailing slash — preserves /team, /blog, /services URLs from static HTML.
}

export default nextConfig
